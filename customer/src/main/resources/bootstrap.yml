server:
  port: 3345
spring:
  application:
    name: customer-app
  cloud:
    config:
      label: main
      name: config
      profile: dev
      uri: http://localhost:3344
eureka:
    client:
      serviceUrl:
        defaultZone=http://eureka:8761/eureka/: