server:
  port: 3346
spring:
  cloud:
    config:
      label: main
      name: config
      profile: dev
      uri: http://localhost:3344
      username: user
      password: f5730234-dbbb-443c-8afd-4cafe4985812
eureka:
    client:
      serviceUrl:
        defaultZone=http://eureka:8761/eureka/:
# 暴露监控端点
management:
  endpoints:
    web:
      exposure:
        include: "*"